// This is a placeholder test to confirm jest is working out of the box.
// Feel free to delete and add your own unit tests here!
import add from "./index";

test('Test placeholder add function', () => {
	expect(add(1,2)).toBe(3);
	expect(add(10,20)).toBe(30);
})